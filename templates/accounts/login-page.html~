{% extends 'base.html' %}

{% block title %}Login - Excel Tips and Tricks{% endblock %}

{% block content %}
<main class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="text-center mb-4">Login to Your Account</h2>

            <!-- Display error message if authentication fails -->
            {% if messages %}
                <div class="alert alert-danger">
                    {% for message in messages %}
                        <p>{{ message }}</p>
                    {% endfor %}
                </div>
            {% endif %}

            <form method="POST">
                {% csrf_token %}
                
                <!-- Username field with placeholder and required attribute -->
                <div class="form-group">
                    <label for="id_username" class="form-label">Username</label>
                    {{ form.username }} <!-- No need to add class here if not using widget_tweaks -->
                </div>
                
                <!-- Password field with placeholder and required attribute -->
                <div class="form-group">
                    <label for="id_password" class="form-label">Password</label>
                    {{ form.password }} <!-- No need to add class here if not using widget_tweaks -->
                </div>

                <form action="{% url 'login' %}" method="get" style="display: inline;">
                    <button type="submit" class="btn-excel mx-2 px-4">Login</button>
                </form>
            </form>

            <p class="text-center mt-3">
                Don't have an account? <a href="{% url 'register' %}" class="text-excel">Register here</a>.
            </p>
        </div>
    </div>
</main>
{% endblock %}