{% extends 'base.html' %}

{% block title %}Register - Excel Tips and Tricks{% endblock %}

{% block content %}
<main class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="text-center mb-4">Create a New Account</h2>
            <form method="POST">
                {% csrf_token %}
                {{ form.as_p }}  <!-- Render form fields dynamically -->
                <div class="form-group text-center">
                    <button type="submit" class="btn btn-excel px-4 py-2">Register</button>
                </div>
            </form>
            <p class="text-center mt-3">
                Already have an account? <a href="{% url 'login' %}" class="text-excel">Login here</a>.
            </p>
        </div>
    </div>
</main>
{% endblock %}