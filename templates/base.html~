{% load static %}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Excel Tips and Tricks{% endblock %}</title>
    
    <!-- Bootstrap CSS via CDN -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Optionally, include a custom CSS file -->
    <link rel="stylesheet" href="{% static 'css/home.css' %}">
    
    {% block extra_head %}{% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">

    <!-- Header and Navigation -->
    <header class="bg-success text-white text-center py-4">
        <h1 class="display-4">{% block header_title %}Welcome to Excel Tips and Tricks{% endblock %}</h1>
        <nav>
            <ul class="nav justify-content-center">
                <li class="nav-item">
                    <a class="nav-link btn-excel mx-2 px-4" href="{% url 'home' %}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn-excel mx-2 px-4" href="{% url 'about' %}">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn-excel mx-2 px-4" href="#">Categories</a>
                </li>
            {% if user.is_authenticated %}
                <form action="{% url 'logout' %}" method="POST" style="display: inline;">
                    {% csrf_token %}
                    <button type="submit" class="btn-excel mx-2 px-4">Logout</button>
                </form>
            {% else %}
                <a href="{% url 'login' %}" class="btn-excel mx-2 px-4">Login</a>
            {% endif %}
            </ul>
        </nav>
    </header>

    {% block content %}{% endblock %}

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-3 mt-auto">
        <p>&copy; 2024 Excel Tips and Tricks</p>
    </footer>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>